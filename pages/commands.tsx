import type { NextPage } from 'next';
import styles from '../styles/Home.module.css';

const Commands: NextPage = () => {
  return (
    <div className={styles.container}>
      <main>Commands Page</main>
    </div>
  );
};

export default Commands;
